export interface AdvancedGraphSettings {
    lineColor: string;
    lineWidth: number;
    lineOpacity: number;
}

export const DEFAULT_SETTINGS: AdvancedGraphSettings = {
    lineColor: '#23b862',
    lineWidth: 2,
    lineOpacity: 0.8
} 
